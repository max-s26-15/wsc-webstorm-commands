<idea-plugin>
    <id>dev.wsc.debug-tool</id>
    <name>wsc Debug Tool</name>
    <vendor>wsc</vendor>
    <version>0.3.0</version>

    <description><![CDATA[
        Adds one tool to the IDE's built-in MCP Server: <code>debug_run_configuration</code>, which starts an
        existing run configuration with the Debug executor, exactly like the Debug button does.
        Used by the <code>wsc</code> command line tool so that <code>wsc name:debug</code> opens a real Debug tab
        instead of a Terminal tab with a bare Node inspector.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.mcpServer</depends>

    <extensions defaultExtensionNs="com.intellij">
        <mcpServer.mcpToolset implementation="dev.wsc.ide.WscDebugToolset"/>
    </extensions>
</idea-plugin>
